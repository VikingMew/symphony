services:
  migrate:
    build:
      args:
        SYMPHONY_EMBED_CODEX: "false"

  symphony:
    build:
      args:
        SYMPHONY_EMBED_CODEX: "false"
    environment:
      SYMPHONY_EXECUTION_MODE: worker
    volumes: !override
      - symphony_logs:/data/logs
      - symphony_workspaces:/data/workspaces
      - gh_config:/home/symphony/.config/gh
      - ssh_home:/home/symphony/.ssh
